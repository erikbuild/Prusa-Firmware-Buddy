# FIFO Coder

This folder contains the infamous modbus FIFO. The code was split into common and decoder and encoder modules, all available as separate libraries, so the FW can only include whatever they need.
