add_library(indx_head STATIC)
target_include_directories(indx_head PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/include)
target_link_libraries(indx_head PUBLIC buddy_utils)
target_link_libraries(indx_head PRIVATE modbus)
target_sources(indx_head PRIVATE src/modbus.cpp)
